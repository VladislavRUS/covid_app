import React from 'react';
import { Text } from 'react-native';
import { SafeView } from '../../components/SafeView';

const Info = () => (
  <SafeView>
    <Text>Info</Text>
  </SafeView>
);

export { Info };
